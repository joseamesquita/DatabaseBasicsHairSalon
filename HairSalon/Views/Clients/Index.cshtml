@{
  Layout = "_Layout";
}
@using System.Collections.Generic;
@using HairSalon.Models;
@model List<HairSalon.Models.Client>;
  
  <div class="text-center">
          <h1>Clients and their Stylist(s) (ex: client || stylist)</h1>
          
          @if (@Model.Count == 0)
          {
            <h2>No Clients have been added yet.</h2>
          } 
            @foreach(Client client in Model)
            {
                <p>@Html.ActionLink($"{client.ClientName}", "Details", new { id = client.ClientId }) || @client.Stylist.StylistName</p>
            }

            <p>@Html.ActionLink("Add Client", "Create")</p>
            <p>@Html.ActionLink("Home", "Index", "Home")</p>
  </div>        